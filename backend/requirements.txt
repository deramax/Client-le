# ClienTara backend — core dependencies (buildable locally without system libs)
Django>=5.0,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
django-filter>=24.0
psycopg[binary]>=3.2
pyotp>=2.9            # MFA (TOTP) — pure python, works locally
qrcode[pil]>=7.4      # optional QR data-uri for MFA enrolment
requests>=2.32        # 4D REST connector
gunicorn>=22.0
whitenoise>=6.7
